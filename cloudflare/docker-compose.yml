services:
  cloudflared:
    image: cloudflare/cloudflared:latest@sha256:89ee50efb1e9cb2ae30281a8a404fed95eb8f02f0a972617526f8c5b417acae2
    env_file:
      - /volume2/FastSSD/docker/cloudflare/.env
    container_name: Cloudflare-Tunnel
    restart: unless-stopped
    command: tunnel run
    # DELETED: environment section
    # (The container will now find TUNNEL_TOKEN automatically from the env_file)
