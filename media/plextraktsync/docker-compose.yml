services:
  plextraktsync:
    image: ghcr.io/taxel/plextraktsync:0.35.0@sha256:7a0c03e629a8b309fafb11a36d4a24f6efc7a9bc5a039d0baf665e872cf766fa
    container_name: PlexTraktSync
    # 👇 CHANGE 1: Use relative path (The runner creates this file right here)
    env_file:
      - .env
    # This tells it to stay running and sync periodically
    command: watch
    volumes:
      - /volume2/FastSSD/docker/plextraktsync/config:/app/config
    environment:
      - PUID=1000
      - PGID=10
      - TZ=Europe/London
    # 👇 CHANGE 2: "on-failure" handles the dependency. 
    # If Plex isn't ready, this crashes, waits, and tries again.
    restart: on-failure:5
