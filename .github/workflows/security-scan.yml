name: Security Scan (Trivy)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * 0'  # Run once a week (Sunday)

jobs:
  build:
    name: Build & Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6 # Updated to latest stable node20 version

      - name: Run Trivy Vulnerability Scanner (IaC)
        uses: aquasecurity/trivy-action@e368e328979b113139d6f9068e03accaed98a518 # 0.34.1 # Pinned to stable release instead of @master
        with:
          scan-type: 'config' # Changed from 'fs' to scan your docker-compose files
          hide-progress: true
          format: 'table'
          exit-code: '1' # Fail the build if High/Critical issues are found
          severity: 'CRITICAL,HIGH'
